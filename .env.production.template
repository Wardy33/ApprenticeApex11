# Production Environment Configuration Template
# This file contains the production environment variables structure
# Set actual values via your deployment platform's environment variables

# Server Configuration
NODE_ENV=production
PORT=3002

# Frontend URL
FRONTEND_URL=https://apprenticeapex.com

# Database Configuration - REQUIRED
# Set these via your deployment platform (Vercel, Netlify, Docker, etc.)
NEON_PROJECT_ID=${NEON_PROJECT_ID}
DATABASE_URL=${DATABASE_URL}

# JWT Configuration - REQUIRED
# Generate a secure 64+ character random string
JWT_SECRET=${JWT_SECRET}

# Stripe Payment Processing - REQUIRED
# Use your live Stripe keys for production
STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY}
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}

# Google OAuth Authentication - REQUIRED
# Use your production OAuth credentials
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}

# Email Service - REQUIRED
# Configure your production email service
EMAIL_SERVICE_API_KEY=${EMAIL_SERVICE_API_KEY}
EMAIL_FROM_ADDRESS=noreply@apprenticeapex.com

# Admin Configuration - REQUIRED
# Generate secure random codes
MASTER_ADMIN_CODE=${MASTER_ADMIN_CODE}
MASTER_ADMIN_SETUP_CODE=${MASTER_ADMIN_SETUP_CODE}

# Security Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
SESSION_SECRET=${SESSION_SECRET}

# AI Moderation Configuration
AI_MODERATION_ENABLED=true
AI_MODERATION_CONFIDENCE_THRESHOLD=0.8
AI_AUTO_SUSPEND_THRESHOLD=0.9

# Monitoring and Logging
LOG_LEVEL=info
MONITORING_ENABLED=true
ERROR_REPORTING_ENABLED=true

# CDN and Assets
CDN_BASE_URL=https://cdn.apprenticeapex.com
UPLOAD_MAX_SIZE=10485760

# Application Limits
MAX_APPLICATIONS_PER_CANDIDATE=50
MAX_JOBS_PER_COMPANY=100
MAX_MESSAGES_PER_CONVERSATION=1000

# Features Flags
ENABLE_VIDEO_INTERVIEWS=true
ENABLE_AI_MATCHING=true
ENABLE_REAL_TIME_CHAT=true
ENABLE_PAYMENT_PROCESSING=true

# DEPLOYMENT INSTRUCTIONS:
# 1. Copy this template to your deployment platform's environment variables
# 2. Generate secure values for all ${VARIABLE_NAME} placeholders
# 3. Never commit actual secrets to version control
# 4. Use your platform's secret management (Vercel/Netlify environment variables, Docker secrets, etc.)